version: '3.7'
services:
  nfs:
    volumes:
      - /data:/data
    ports:
      - 2049:2049
      
    networks:
     - proxy
     - internal

networks:
  proxy:
    driver: overlay
    external: true

  internal:
    driver: overlay
    external: true
